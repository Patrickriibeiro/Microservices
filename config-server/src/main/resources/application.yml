server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Patrickriibeiro/config-server
          default-label: main
          search-paths:
          - 'greeting-service*'
          #username: 
          #password: 
